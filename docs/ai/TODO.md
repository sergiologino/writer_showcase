# TODO — рабочий бэклог

Канонический план и сверка с ТЗ: [`TZ_BACKLOG.md`](../TZ_BACKLOG.md), текущий снимок — [`CURRENT_STATE.md`](./CURRENT_STATE.md). Здесь — **что в очереди** и **что взять следующим**; по завершении пункты переносятся в [`CHANGELOG_AI.md`](./CHANGELOG_AI.md) и кратко отражаются в `CURRENT_STATE.md` (см. [`CONVENTIONS.md`](./CONVENTIONS.md)).

## В работу следующим (по приоритету)

1. **Интеграция с нейросетями (углубление)**  
   - Уже есть: noteapp-ai-integration, `/api/ai/invoke` → `POST /api/ai/process`, промпты workspace.  
   - Далее: сценарии в UI (не только `chat`), типы запросов под продукт, согласование с `AiRequestDTO`/`requestType`, при необходимости — потоки правок из редактора, варианты текста на канал совместно с `PostVariant` (см. `TZ_BACKLOG.md`).

## Очередь (ещё не закрыта по `CURRENT_STATE` / `TZ_BACKLOG`)

2. **Публикация в TG/VK/MAX/каналах (углубление):** обновление сообщения в канале при редактировании поста, шифрование секретов at rest, расширенный формат (медиа, превью); при мульти-инстанс — блокировки `SKIP LOCKED` или воркер. MAX: при необходимости — вложения через `POST /uploads` API.  
3. **Фоновые задачи и кэш (углубление):** метрики/квоты, job-типы по ТЗ, при необходимости DLQ.  
4. **Публичный URL автора (ADR-008):** `/{author_nickname}/...` вместо `workspace.slug` в путях.  
5. **Публикация «как в ТЗ»:** чекбоксы каналов у поста, `PostChannelBinding`, статусы, `external_url`, republish.  
6. **Расписание:** `scheduled_at`, отложенная публикация, календарь.  
7. **Прочее бэклог:** RSS, аналитика, роли RBAC, шифрование секретов, RSS/OG, дополнительные внешние площадки — по `TZ_BACKLOG.md`.

## Бэклог (крупнее, без жёсткого порядка)

| Тема | Примечание |
|------|------------|
| Миграция на Java 21 | Когда появится JDK 21 (см. ADR-004) |
| RSS, расширенная аналитика | `TZ_BACKLOG.md` |
| X, Facebook, Reddit, … | Адаптеры по мере приоритета |

---

*Новые крупные отказы/решения — в [`DECISIONS.md`](./DECISIONS.md); детализация этапов — в `TZ_BACKLOG.md`.*
